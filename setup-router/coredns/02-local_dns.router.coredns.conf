. {
  # health should be imported only once
  health
  hosts {
    fallthrough
  }
  # forward . /etc/resolv.conf
  import default_dns
  # log
  # loadbalance
  reload
}

home.shkits.com {
  hosts {
    172.18.1.10 home.shkits.com
    fallthrough
  }
  import local_dns
}
