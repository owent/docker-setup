# Plugin Configuration
# 全局插件配置，在此列出的插件会对所有请求生效
#
# 注意: remap.config 中使用 @plugin= 语法的插件是针对特定规则的
# 此文件用于全局插件
#
# 示例:
# stats_over_http.so
# healthchecks.so /etc/trafficserver/healthchecks.config

# 压缩插件 - 支持 gzip, deflate, br (Brotli)
# 如果源站返回未压缩内容，ATS 可以压缩后返回给客户端
# compress.so
