# Apache Traffic Server 缓存分区配置 (hosting.config)
#
# 语法:
#   hostname=<域名> volume=<卷号>
#   domain=<顶级域名> volume=<卷号>
#
# 默认情况下所有域名共享同一个缓存卷
# 如需要为特定域名分配独立缓存空间，可在此配置
#
# 注意: 需要先在 volume.config 中定义卷

# 目前使用默认配置 - 所有域名共享缓存
# 如需分离，取消注释并配置:

# 高优先级缓存 (GitHub/Maven/npm 包文件)
# hostname=release-assets.githubusercontent.com volume=1
# hostname=repo1.maven.org volume=1
# hostname=files.pythonhosted.org volume=1
# hostname=registry.npmjs.org volume=1

# CDN 缓存
# domain=cloudflare.com volume=2
# domain=jsdelivr.net volume=2
