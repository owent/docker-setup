# HAProxy Monitor Configuration
# 飞书 Webhook URL（必需）
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/YOUR_WEBHOOK_TOKEN

# HAProxy Socket 路径（可选，默认会尝试通过容器获取）
# HAPROXY_SOCKET=/var/run/haproxy.sock

# 状态文件存储路径（可选）
# STATE_FILE=/tmp/haproxy-monitor-state.json

# HAProxy Stats URL（可选，如果使用 HTTP stats）
# HAPROXY_STATS_URL=http://127.0.0.1:8404/stats
